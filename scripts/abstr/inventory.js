class Inventory extends ItemTable {
    constructor(cols, rows) {
        super(cols, rows, invStr);
    }

    handleDouble(x, y, d, s) {
        //todo: handle these
    }

    handleDrag(x, y, d, s) {

    }

    handlePress(x, y, d, s) {

    }
}
